
spring:
  application:
    name: messenger
  datasource:
    username: postgres
    password: 0000
    url: jdbc:postgresql://localhost:5432/messenger
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: validate
        show_sql: true
        format_sql: true
